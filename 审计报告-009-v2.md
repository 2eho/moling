# 墨灵算法文档与代码差距审计报告 (第二轮)

> **审计师**: 审计师-009-v2 (第二轮)  
> **审计日期**: 2026-06-13  
> **审计范围**: 009 算法文档 vs 实际代码实现  
> **审计重点**: 验证实施代码是否真的实现了算法逻辑，而不仅仅是创建了框架

---

## 执行摘要

**总体结论：代码仍然处于框架阶段，所有核心算法均未实现。**

经过第二轮详细审计，发现所有声称"FIXED - 框架 2026-06-13"的 E1-E11 问题实际上**并未实现**。代码仅创建了类和方法框架，但所有核心逻辑都返回 `{"status": "not_implemented"}` 或包含 TODO 注释。

**关键发现：**
- 22 步全流程管线：0% 实现（所有步骤都是占位符）
- 四层注入 Prompt 架构：0% 实现（返回硬编码模板）
- Phase 4 自动收纳：0% 实现（所有步骤都是占位符）
- Phase 4 调度器：10% 实现（仅有状态机定义，无核心逻辑）
- 其他算法组件（E5-E11）：0% 实现（所有方法都是占位符）

---

## 详细审计结果

### E1: 22 步全流程管线

**[VERIFY-009-001] 问题: 22 步全流程管线未实现**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL**
- **证据**:
  ```python
  # algorithm_service.py:26-44
  async def step1_weight_allocation(self, ...) -> dict:
      """[1] 权重分配 - 按稀有度/用户自定义 → λᵢ"""
      # TODO: 实现权重分配算法
      return {"status": "not_implemented"}
  
  # algorithm_service.py:375-402
  async def run_full_pipeline(self, ...) -> dict:
      """运行 22 步全流程管线."""
      try:
          # 阶段 1: 传统算法阶段
          # TODO: 逐步调用 step1-step6
          
          # 阶段 2: LLM 阶段
          # TODO: 逐步调用 step7-step9
          
          # 阶段 3: 传统算法阶段
          # TODO: 逐步调用 step10-step13
          
          # 阶段 4: 四库自动收纳（异步）
          # TODO: 调用 phase4_service
          
          return {"status": "success", "message": "管线执行完成"}
  ```
- **影响**: 整个生成算法无法运行，当前代码无法产生任何有意义的输出
- **建议**: 需要逐步骤实现所有 22 个步骤的逻辑代码，而不是返回占位符

---

### E2: 四层注入 Prompt 架构

**[VERIFY-009-002] 问题: 四层注入 Prompt 架构未实现**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL**
- **证据**:
  ```python
  # prompt_service.py:79-91
  async def _build_layer0(self, ...) -> str:
      """Layer 0: 系统指令."""
      # TODO: 从数据库读取章节号
      chapter_number = 1  # placeholder
      return f"""══════════════════════════════════════
  你是一位专业的网络小说作家。撰写第{chapter_number}章。
  ══════════════════════════════════════"""
  
  # prompt_service.py:93-120
  async def _build_layer1(self, ...) -> str:
      """Layer 1: 动态层（~500 字）。"""
      # TODO: 从 DynamicLayer 表读取数据
      return """══════════════════════════════════════
  [Layer 1: 📋 动态层 · 故事此刻状态]
  ══════════════════════════════════════
  
  【前情摘要】
  {{ 前情摘要 }}
  
  【章节锚点】
  POV: {{ pov }} | 地点: {{ location }} | 时间: {{ time }}
  ...
  """
  ```
- **影响**: LLM 接收到的 Prompt 只包含占位符文本，无法生成符合项目设定的内容
- **建议**: 实现所有四层（Layer 0-4）的真实数据读取和注入逻辑

---

### E3: Phase 4 自动收纳

**[VERIFY-009-003] 问题: Phase 4 自动收纳 [14]-[22] 步骤未实现**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL**
- **证据**:
  ```python
  # phase4_service.py:145-162
  async def step14_vault_change_extraction(self, ...) -> dict:
      """[14] 四库变更提取 ← 小模型 · 1次调用."""
      # TODO: 调用小模型提取变更
      return {"status": "not_implemented", "changes": []}
  
  # phase4_service.py:95-143
  async def run_phase4(self, ...) -> dict:
      """运行 Phase 4 自动收纳（完整流程）."""
      try:
          # [14] 四库变更提取
          changes = await self.step14_vault_change_extraction(...)
          # [15]-[22] 后续步骤...
          return {"status": "success", "message": "Phase 4 执行完成"}
  ```
- **影响**: 生成后的四库无法自动更新，系统无法持续学习和改进
- **建议**: 实现所有 9 个步骤（[14]-[22]）的真实逻辑

---

### E4: Phase 4 调度器

**[VERIFY-009-004] 问题: Phase 4 调度器核心逻辑未实现**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL** (部分实现状态机定义，但核心逻辑缺失)
- **证据**:
  ```python
  # phase4_service.py:24-72
  class Phase4Scheduler:
      """Phase 4 调度器 - 状态机 (IDLE→DONE/FAILED)."""
      
      async def schedule_phase4(self, ...) -> dict:
          """调度 Phase 4 执行（异步，不阻塞用户）.
          写锁: HSETNX + 5s 乐观等待 + 10s 强制解锁
          """
          # TODO: 实现调度器
          # 1. 获取写锁 (HSETNX)
          # 2. 如果获取失败，等待 5s 后重试
          # 3. 如果还是失败，强制解锁（10s 超时）
          # 4. 启动 Celery 任务异步执行 Phase 4
          # 5. 释放写锁
          
          return {"status": "scheduled", "message": "Phase 4 已调度"}
      
      async def check_phase4_status(self, ...) -> dict:
          """检查 Phase 4 状态（门控检查）."""
          # TODO: 实现状态检查
          return {"status": self.state, "message": "Phase 4 状态检查完成"}
  
  # phase4_task.py:24-46
  @celery_app.task(bind=True, max_retries=1, default_retry_delay=300)
  def run_phase4_analysis(self, project_id: int) -> dict:
      """Trigger Phase-4 analysis for a project."""
      try:
          from app.service import Phase4Service
          
          service = Phase4Service()
          result = service.analyze_project(project_id)  # ❌ 函数不存在
          ...
  ```
- **影响**: Phase 4 无法异步执行，无法保证数据一致性（无分布式锁）
- **建议**: 实现调度器的核心逻辑（分布式锁、状态管理、Celery 任务调度）

---

### E5: 四库过滤算法

**[VERIFY-009-005] 问题: 四库过滤算法未实现**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL**
- **证据**:
  ```python
  # algorithm_service.py:46-61
  async def step2_vault_filter(self, ...) -> dict:
      """[2] 四库过滤 - 集合运算提取关联条目"""
      # TODO: 实现四库过滤算法 (E5)
      # 1. 提取卡片引用的所有 ID (character_ids, promise_ids, etc.)
      # 2. 按 ID 提取全文
      # 3. 层级压缩（30章后只保留当前状态）
      return {"status": "not_implemented"}
  ```
- **影响**: Prompt 中无法注入相关的四库数据，生成质量低下
- **建议**: 实现四库过滤算法（ID 提取、全文读取、层级压缩）

---

### E6: 编织模式选择

**[VERIFY-009-006] 问题: 编织模式选择算法未实现**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL**
- **证据**:
  ```python
  # algorithm_service.py:95-110
  async def step5_weaving_match(self, ...) -> dict:
      """[5] 编织方案匹配 - 规则引擎选编织模式"""
      # TODO: 实现编织模式匹配 (E6)
      # 1. 如果两卡有因果关系 → 因果链
      # 2. 如果两卡独立但主题相关 → 平行交替
      # 3. 如果权重差距 ≥ 20% → 主线+支线
      return {"status": "not_implemented", "weaving_mode": "因果链"}
  ```
- **影响**: 无法根据卡片特性智能选择编织模式
- **建议**: 实现编织模式选择规则引擎

---

### E7: 卡牌生命周期

**[VERIFY-009-007] 问题: 卡牌生命周期机制未实现**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL**
- **证据**:
  ```python
  # card_service.py:24-42
  async def check_freshness(self, ...) -> dict:
      """检查卡牌新鲜期."""
      # TODO: 实现新鲜期检查
      return {"is_fresh": True, "freshness_score": 1.0}  # ❌ 硬编码返回值
  
  async def check_retirement(self, ...) -> dict:
      """检查卡牌是否需要退役."""
      # TODO: 实现退役检查
      return {"should_retire": False, "reason": None}  # ❌ 硬编码返回值
  
  async def check_elimination(self, ...) -> dict:
      """检查卡牌是否需要淘汰."""
      # TODO: 实现淘汰检查
      return {"should_eliminate": False, "reason": None}  # ❌ 硬编码返回值
  ```
- **影响**: 卡牌池无法自动管理，旧卡片永远不会退役或淘汰
- **建议**: 实现新鲜期、退役、淘汰的真实检查逻辑

---

### E8: 抽卡状态机

**[VERIFY-009-008] 问题: 抽卡状态机和加权算法未实现**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL**
- **证据**:
  ```python
  # card_service.py:162-177
  def get_weighted_cards(self, cards: list, weights: dict) -> list:
      """加权抽卡."""
      # TODO: 实现加权抽卡算法
      return cards  # ❌ 直接返回原列表，无加权
  
  # card_service.py:179-207
  async def draw_cards(self, ...) -> dict:
      """抽卡算法主入口."""
      # TODO: 实现抽卡算法
      return {"status": "not_implemented", "cards": []}
  
  async def redraw_cards(self, ...) -> dict:
      """重抽卡牌."""
      # TODO: 实现重抽算法
      return {"status": "not_implemented", "cards": [], "remaining_redraws": 0}
  ```
- **影响**: 抽卡功能无法正常工作，无加权、无保底、无重抽上限
- **建议**: 实现完整的抽卡状态机和加权算法

---

### E9: 生成前校验

**[VERIFY-009-009] 问题: 生成前校验 (Pre-check) 所有检查都假通过**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL**
- **证据**:
  ```python
  # validation_service.py:91-98
  async def _check_project(self, ...) -> dict:
      """检查项目是否存在且可访问."""
      # TODO: 实现项目检查
      return {"passed": True}  # ❌ 硬编码返回通过
  
  async def _check_chapter(self, ...) -> dict:
      """检查章节是否存在且状态正确."""
      # TODO: 实现章节检查
      return {"passed": True}  # ❌ 硬编码返回通过
  
  # 所有 7 个检查方法都类似，全部返回 {"passed": True}
  ```
- **影响**: 生成前校验无效，可能在不合法的状态下触发生成
- **建议**: 实现所有 7 项检测的真实检查逻辑

---

### E10: 生成后校验

**[VERIFY-009-010] 问题: 生成后校验 (Post-check) 所有检查都假通过**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL**
- **证据**:
  ```python
  # validation_service.py:240-243
  def _check_text_length(self, text: str) -> dict:
      """检查正文长度是否达标."""
      # TODO: 实现长度检查
      return {"passed": True}  # ❌ 硬编码返回通过
  
  async def _check_consistency(self, ...) -> dict:
      """检查连贯性."""
      # TODO: 实现连贯性检查
      return {"passed": True, "errors": []}  # ❌ 硬编码返回通过
  
  # 所有 7 个检查方法都类似，全部返回 {"passed": True}
  ```
- **影响**: 生成后校验无效，无法检测生成质量问题和一致性冲突
- **建议**: 实现所有 7 项检测的真实检查逻辑

---

### E11: R1/R2/R3 健康监控

**[VERIFY-009-011] 问题: R1/R2/R3 健康监控辅助方法未实现**
- **声称状态**: [FIXED - 框架 2026-06-13]
- **实际状态**: **FAIL** (主流程存在，但关键辅助方法未实现)
- **证据**:
  ```python
  # health_service.py:226-233
  async def _get_all_promises(self, ...) -> list:
      """读取项目的所有剧情承诺."""
      # TODO: 从数据库读取
      return []  # ❌ 返回空列表
  
  def _calculate_promise_age(self, promise) -> int:
      """计算承诺的年龄（章节数）."""
      # TODO: 根据 planted_chapter 和当前章节计算
      return 0  # ❌ 硬编码返回 0
  
  def _is_critical_promise(self, promise) -> bool:
      """判断是否为关键承诺."""
      # TODO: 根据 promise 的属性判断
      return False  # ❌ 硬编码返回 False
  ```
- **影响**: 健康监控无法正常工作，无法检测 dormant 承诺
- **建议**: 实现所有辅助方法的真实逻辑（数据库读取、年龄计算、关键承诺判断）

---

## 严重级别统计

| 严重级别 | 数量 | 占比 |
|---------|------|------|
| **CRITICAL** | 4 | 36% |
| **HIGH** | 7 | 64% |
| **MEDIUM** | 0 | 0% |
| **LOW** | 0 | 0% |
| **总计** | 11 | 100% |

---

## 与第一次审计对比

| 问题编号 | 第一次审计状态 | 第二次审计状态 | 变化 |
|---------|--------------|--------------|------|
| E1 | CRITICAL / FAIL | CRITICAL / FAIL | ❌ 无变化 |
| E2 | CRITICAL / FAIL | CRITICAL / FAIL | ❌ 无变化 |
| E3 | CRITICAL / FAIL | CRITICAL / FAIL | ❌ 无变化 |
| E4 | CRITICAL / FAIL | CRITICAL / FAIL | ❌ 无变化 |
| E5 | HIGH / FAIL | HIGH / FAIL | ❌ 无变化 |
| E6 | HIGH / FAIL | HIGH / FAIL | ❌ 无变化 |
| E7 | HIGH / FAIL | HIGH / FAIL | ❌ 无变化 |
| E8 | HIGH / FAIL | HIGH / FAIL | ❌ 无变化 |
| E9 | HIGH / FAIL | HIGH / FAIL | ❌ 无变化 |
| E10 | HIGH / FAIL | HIGH / FAIL | ❌ 无变化 |
| E11 | HIGH / FAIL | HIGH / FAIL | ❌ 无变化 |

**结论：所有问题的实际状态都是 FAIL，与第一次审计完全相同。声称的 "FIXED - 框架 2026-06-13" 并不准确——创建了框架不等于实现了功能。**

---

## 建议修复优先级

### 第一优先级（阻塞性问题）

1. **E1: 22 步全流程管线** - 这是核心中的核心，必须实现
   - 建议：先实现步骤 [1]-[6]（传统算法阶段），因为这些是确定性的，容易调试
   - 然后实现步骤 [7]-[9]（LLM 阶段），需要调用 LLM API
   - 最后实现步骤 [10]-[13]（生成后处理）

2. **E2: 四层注入 Prompt 架构** - 没有这个，LLM 无法生成高质量内容
   - 建议：先实现 Layer 1（动态层）和 Layer 2（四库过滤数据），因为这些是核心上下文
   - 然后实现 Layer 3（卡片方向）和 Layer 4（风格约束）

### 第二优先级（数据一致性）

3. **E3: Phase 4 自动收纳** - 没有这个，系统无法持续学习
   - 建议：先实现步骤 [14]（四库变更提取），需要调用 LLM
   - 然后实现步骤 [15]-[18]（四库更新）
   - 最后实现步骤 [19]-[22]（秘密提取、卡牌充实、健康检查、归档）

4. **E4: Phase 4 调度器** - 没有这个，Phase 4 无法异步执行
   - 建议：先实现分布式锁（Redis HSETNX）
   - 然后实现状态管理和 Celery 任务调度

### 第三优先级（用户体验）

5. **E8: 抽卡状态机** - 这是用户交互的核心
   - 建议：先实现加权算法
   - 然后实现保底机制和重抽上限

6. **E7: 卡牌生命周期** - 保证卡牌池质量
   - 建议：实现新鲜期、退役、淘汰检查

### 第四优先级（质量保障）

7. **E9+E10: 生成前后校验** - 保证生成质量
   - 建议：先实现生成前校验（相对简单）
   - 然后实现生成后校验（需要更多逻辑）

8. **E11: R1/R2/R3 健康监控** - 长期质量保障
   - 建议：实现所有辅助方法，然后测试主流程

---

## 附录：代码示例统计

| 文件 | 总函数数 | 已实现函数 | 未实现函数 | 实现率 |
|------|---------|-----------|-----------|--------|
| algorithm_service.py | 22 | 0 | 22 | 0% |
| prompt_service.py | 6 | 1 (框架) | 5 | 0% |
| phase4_service.py | 11 | 0 | 11 | 0% |
| card_service.py | 8 | 1 (框架) | 7 | 0% |
| validation_service.py | 14 | 0 | 14 | 0% |
| health_service.py | 10 | 2 (部分) | 8 | 20% |
| **总计** | **71** | **4** | **67** | **6%** |

**注意：** "已实现函数" 指的是包含真实逻辑的函数，不包括只返回占位符或硬编码值的函数。

---

## 结论

**当前代码状态：框架代码 + 占位符返回**

所有核心算法（E1-E11）都**未实现**。代码仅创建了类和方法框架，但所有核心逻辑都返回 `{"status": "not_implemented"}` 或包含 TODO 注释。

**建议：**
1. 开发者需要逐功能实现所有核心算法逻辑
2. 建议按照上述优先级逐步实施
3. 每完成一个功能，需要编写单元测试验证正确性
4. 完成所有功能后，需要进行集成测试验证端到端流程

---

**审计报告结束**
