# 墨灵项目 - 整合测试检查清单

## 测试准备

### 1. 环境准备
- [ ] 后端虚拟环境已激活（`.venv`或`venv310`）
- [ ] 前端`node_modules`已安装（`npm install`）
- [ ] 数据库文件存在（`moling.db` for SQLite）
- [ ] Redis可选（失败会优雅降级）

### 2. 配置文件检查
- [ ] 后端`.env`文件存在且配置正确
  - `DATABASE_URL=sqlite:///./moling.db`
  - `ENVIRONMENT=development`
  - `CORS_ORIGINS`包含前端URL
- [ ] 前端`.env.local`文件存在
  - `NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1`

---

## 后端启动测试

### 3. 启动后端服务
```bash
cd C:\Users\Admin\Desktop\MolingProject\moling-server
python -m uvicorn app.main:app --reload --port 8000
```

- [ ] 启动成功（无错误）
- [ ] 数据库表已创建（看到"[OK] Database tables verified"）
- [ ] Redis连接成功或优雅降级（看到"[OK] Redis connected"或"[WARN] Redis unavailable"）
- [ ] Swagger文档可访问：http://localhost:8000/docs

### 4. 后端API测试（使用Swagger UI或curl）

#### 4.1 认证API（无需Token）
- [ ] `POST /api/v1/auth/register` - 注册新用户
- [ ] `POST /api/v1/auth/login` - 登录获取Token
- [ ] `GET /api/v1/health` - 健康检查（无需认证）

#### 4.2 项目API（需Token）
- [ ] `POST /api/v1/projects` - 创建项目
- [ ] `GET /api/v1/projects` - 获取项目列表
- [ ] `GET /api/v1/projects/{project_id}` - 获取单个项目
- [ ] `PUT /api/v1/projects/{project_id}` - 更新项目
- [ ] `DELETE /api/v1/projects/{project_id}` - 删除项目
- [ ] `GET /api/v1/projects/stats` - 获取项目统计

#### 4.3 章节API（需Token）
- [ ] `POST /api/v1/projects/{project_id}/chapters` - 创建章节
- [ ] `GET /api/v1/projects/{project_id}/chapters` - 获取章节列表
- [ ] `GET /api/v1/projects/{project_id}/chapters/{chapter_id}` - 获取单个章节
- [ ] `PUT /api/v1/projects/{project_id}/chapters/{chapter_id}` - 更新章节
- [ ] `POST /api/v1/projects/{project_id}/chapters/reorder` - 重新排序
- [ ] `DELETE /api/v1/projects/{project_id}/chapters/{chapter_id}` - 删除章节

#### 4.4 卡牌API（需Token）
- [ ] `GET /api/v1/projects/{project_id}/cards/pool` - 获取卡池
- [ ] `POST /api/v1/projects/{project_id}/cards/draw` - 抽卡
- [ ] `POST /api/v1/projects/{project_id}/cards/redraw` - 重新抽卡

#### 4.5 生成API（需Token）
- [ ] `POST /api/v1/projects/{project_id}/generation` - 开始生成
- [ ] `GET /api/v1/generation/{task_id}` - 查询任务状态
- [ ] `DELETE /api/v1/generation/{task_id}` - 取消任务

#### 4.6 四库API（需Token）
- [ ] `GET /api/v1/projects/{project_id}/vault/characters` - 获取角色库
- [ ] `POST /api/v1/projects/{project_id}/vault/characters` - 创建角色
- [ ] `GET /api/v1/projects/{project_id}/vault/timeline` - 获取时间线
- [ ] `POST /api/v1/projects/{project_id}/vault/timeline` - 创建时间线事件
- [ ] `GET /api/v1/projects/{project_id}/vault/plot-promises` - 获取伏笔
- [ ] `POST /api/v1/projects/{project_id}/vault/plot-promises` - 创建伏笔
- [ ] `GET /api/v1/projects/{project_id}/vault/world` - 获取世界观
- [ ] `POST /api/v1/projects/{project_id}/vault/world` - 创建世界观条目

#### 4.7 通知API（需Token，贝洛奇实现后测试）
- [ ] `GET /api/v1/notifications` - 获取通知列表
- [ ] `PUT /api/v1/notifications/{notification_id}/read` - 标记已读
- [ ] `DELETE /api/v1/notifications/{notification_id}` - 删除通知

#### 4.8 设置API（需Token，贝洛奇实现后测试）
- [ ] `GET /api/v1/settings/me` - 获取个人设置
- [ ] `PUT /api/v1/settings/me` - 更新个人设置
- [ ] `POST /api/v1/settings/change-password` - 修改密码

---

## 前端启动测试

### 5. 启动前端服务
```bash
cd C:\Users\Admin\Desktop\MolingProject\moling-web
npm run dev
```

- [ ] 启动成功（无错误）
- [ ] Landing页面可访问：http://localhost:3000
- [ ] 无TypeScript类型错误（`npm run build`成功）

### 6. 前端页面测试（手动）

#### 6.1 认证流程
- [ ] Landing页面加载正常
- [ ] 点击"登录"按钮跳转到`/auth`页面
- [ ] 登录表单提交成功（调用`POST /api/v1/auth/login`）
- [ ] 登录后Token正确存储（localStorage）
- [ ] 注册表单提交成功（调用`POST /api/v1/auth/register`）
- [ ] 未认证时访问受保护页面重定向到登录页

#### 6.2 项目管理
- [ ] 项目列表页面加载正常（`/projects`）
- [ ] 显示已创建的项目（调用`GET /api/v1/projects`）
- [ ] 点击"新建项目"跳转到`/projects/new`
- [ ] 创建项目表单提交成功（调用`POST /api/v1/projects`）
- [ ] 点击项目卡片进入工作空间（`/workspace/{project_id}`）

#### 6.3 工作空间
- [ ] 工作空间页面加载正常（三栏布局）
- [ ] 左侧栏显示章节列表（调用`GET /api/v1/.../chapters`）
- [ ] 中间编辑器显示章节内容
- [ ] 右侧栏显示卡牌（调用`GET /api/v1/.../cards/pool`）
- [ ] 点击"抽卡"按钮打开卡牌模态框
- [ ] 抽卡成功（调用`POST /api/v1/.../cards/draw`）
- [ ] 点击"生成"按钮开始AI生文（调用`POST /api/v1/.../generation`）
- [ ] 生成进度条显示正常（轮询`GET /api/v1/generation/{task_id}`）

#### 6.4 四库管理
- [ ] 四库页面加载正常（`/vaults/{project_id}`）
- [ ] 角色库显示正常（调用`GET /api/v1/.../vault/characters`）
- [ ] 时间线显示正常（调用`GET /api/v1/.../vault/timeline`）
- [ ] 伏笔显示正常（调用`GET /api/v1/.../vault/plot-promises`）
- [ ] 世界观显示正常（调用`GET /api/v1/.../vault/world`）
- [ ] 创建/编辑/删除操作正常

#### 6.5 其他页面
- [ ] 设置页面加载正常（`/settings`）
- [ ] 通知页面加载正常（`/notifications`）
- [ ] 定价页面加载正常（`/pricing`）
- [ ] 管理页面加载正常（`/admin`，需管理员权限）

---

## 集成测试

### 7. 端到端流程测试

#### 7.1 完整创作流程
1. [ ] 注册新账号
2. [ ] 创建新项目（玄幻小说）
3. [ ] 创建章节（第一章）
4. [ ] 抽取卡牌（选择灵感）
5. [ ] 开始AI生文（基于卡牌）
6. [ ] 查看生成结果
7. [ ] 在四库中创建角色
8. [ ] 再次生成（卡牌包含新角色）

#### 7.2 错误处理测试
- [ ] 网络错误时显示错误提示（Toast通知）
- [ ] API返回错误时显示错误信息
- [ ] 认证失败时重定向到登录页
- [ ] 输入验证错误时显示表单错误

---

## 性能测试

### 8. 基础性能检查
- [ ] 后端API响应时间 < 500ms（简单查询）
- [ ] 后端API响应时间 < 5s（AI生文，异步任务）
- [ ] 前端页面加载时间 < 2s（首次加载）
- [ ] 前端页面切换时间 < 500ms（客户端导航）

---

## 部署前检查

### 9. 生产环境准备
- [ ] 所有API端点都有认证保护（除了`/health`和`/auth/*`）
- [ ] 所有用户输入都经过验证（使用Pydantic schemas）
- [ ] 错误信息不泄露敏感信息（使用统一错误格式）
- [ ] CORS配置正确（生产环境不使用`*`）
- [ ] Secret Key已更换（不使用默认的）
- [ ] 数据库已运行Alembic迁移（生产环境）

---

## 测试报告模板

### 测试结果汇总
- **后端API测试**：XX/XX 通过
- **前端页面测试**：XX/XX 通过
- **集成测试**：XX/XX 通过
- **性能测试**：通过/失败

### 发现的问题
1. 问题描述 -> 严重程度 -> 负责人 -> 状态
2. ...

### 下一步行动
1. 修复高优先级Bug
2. 完善文档
3. 准备部署

---

**最后更新：** 2026-06-14 by 郝交付(交付总监)
