# 算法文档校正报告
> 生成时间：2026-06-14
> 对比基准：实际代码 vs 设计文档

---

## 一、实际已实现但文档可能未详细记录的部分

### 1.1 `novel_disector/core/` 底层模块

这些模块是拆书引擎的"底层实现"，文档中记录了算法设计（A1-A5），但可能没有详细记录这些模块的功能。

| 模块 | 功能 | 文档状态 |
|------|------|---------|
| `cleaner.py` | HTML 清洗、章节内容清洗 | ❓ 未明确记录 |
| `fetcher.py` | 数据获取（Fetcher、AntiCrawlConfig、FetchResult） | ❓ 未明确记录 |
| `extractor.py` | 内容提取（extract_content、ExtractResult） | ❓ 未明确记录 |
| `toc_crawler.py` | 目录爬取（TOCFetcher、ChapterBatchCrawler、ChapterLink、CrawlProgress） | ❓ 未明确记录 |
| `style_analyzer.py` | 文风分析（StyleFingerprint、analyze_style） | ✅ 已记录（§9.5） |
| `style_prompt_builder.py` | 文风 Prompt 注入（fingerprint_to_prompt、fingerprint_to_compact） | ✅ 已记录 |

**建议**：
- 如果这些模块的功能已经在 §9 中描述了（例如，A1 步骤中描述了"分段检测"，对应 `cleaner.py` 的功能），则不需要补充。
- 如果文档中没有提到这些模块的名称，可以考虑在 §9 中添加一个"实现模块清单"。

---

### 1.2 卡牌生命周期管理

根据 CHANGELOG-2026-06-13.md，卡牌生命周期管理（新鲜期、退役、淘汰检查）已经实现。

**文档状态**：需要检查 §6（卡牌池 API）中是否详细记录了卡牌生命周期的状态转换。

---

### 1.3 健康监控规则

根据 CHANGELOG-2026-06-13.md，健康监控规则（R1/R2/R3）已经实现。

**文档状态**：§5.14（健康监控 API）中记录了 API，但需要检查是否详细记录了 R1/R2/R3 的规则逻辑。

---

## 二、文档记录了但未实现的部分

### 2.1 导入引擎（§10）

文档 §10 记录了"连载书导入引擎"，但实际代码中可能有部分未实现。

**实际代码状态**：需要检查 `app/ingest/` 目录中的实现状态。

---

## 三、需要执行的校正操作

### 3.1 补充底层模块清单

在 §9 中添加一个"实现模块清单"，列出 `novel_disector/core/` 中的所有模块及其功能。

### 3.2 补充卡牌生命周期详细文档

在 §6 中补充卡牌生命周期的状态转换图和实现细节。

### 3.3 补充健康监控规则详细文档

在 §5.14 中补充 R1/R2/R3 的规则逻辑和实现细节。

### 3.4 检查导入引擎实现状态

检查 `app/ingest/` 目录中的实现状态，更新 §10。

---

## 四、执行计划

1. **阶段 1**：补充 `novel_disector/core/` 模块清单到 §9
2. **阶段 2**：补充卡牌生命周期详细文档到 §6
3. **阶段 3**：补充健康监控规则详细文档到 §5.14
4. **阶段 4**：检查导入引擎实现状态，更新 §10

---

**请确认**：
1. 这个报告是否准确？
2. 你希望我立即开始校正，还是先让你审查？
3. 对于"底层模块清单"，你希望详细记录每个模块的功能，还是只列出模块名称？

告诉我你的选择，我会相应调整操作。
