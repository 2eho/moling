# 后端设计文档校正报告
> 生成时间：2026-06-14
> 对比基准：实际代码 vs 设计文档

---

## 一、实际已实现但文档未记录的 API

### 1.1 Generation API（3个端点）
| 方法 | 路径 | 功能 | 位置 |
|------|------|------|------|
| POST | `/api/v1/generation` | 创建生文任务 | `app/router/generation.py` |
| GET | `/api/v1/generation/{task_id}` | 查询任务状态 | `app/router/generation.py` |
| DELETE | `/api/v1/generation/{task_id}` | 取消任务 | `app/router/generation.py` |

**文档状态**：文档中将生文功能放在 Chapters API 中（`POST /chapters/:id/generate`），但实际代码是独立的 Generation API。

---

### 1.2 Phase 4 API（5个端点）
| 方法 | 路径 | 功能 | 位置 |
|------|------|------|------|
| GET | `/api/v1/phase4/suggestions/{chapter_id}` | 获取收纳建议 | `app/router/phase4.py` |
| POST | `/api/v1/phase4/apply` | 应用收纳 | `app/router/phase4.py` |
| GET | `/api/v1/phase4/tasks/{task_id}` | 查询任务状态 | `app/router/phase4.py` |
| GET | `/api/v1/phase4/chapters/{chapter_id}/tasks` | 章节任务列表 | `app/router/phase4.py` |
| GET | `/api/v1/phase4/projects/{project_id}/tasks` | 项目任务列表 | `app/router/phase4.py` |

**文档状态**：文档中将 Phase 4 功能放在 Chapters API 中（`POST /chapters/:id/confirm`），但实际代码是独立的 Phase4 API。

---

### 1.3 Template API（6个端点）
| 方法 | 路径 | 功能 | 位置 |
|------|------|------|------|
| GET | `/api/v1/templates` | 模板列表 | `app/router/template.py` |
| GET | `/api/v1/templates/{template_id}` | 模板详情 | `app/router/template.py` |
| POST | `/api/v1/templates` | 创建模板 | `app/router/template.py` |
| PUT | `/api/v1/templates/{template_id}` | 更新模板 | `app/router/template.py` |
| DELETE | `/api/v1/templates/{template_id}` | 删除模板 | `app/router/template.py` |
| POST | `/api/v1/templates/{template_id}/create-project` | 从模板创建项目 | `app/router/template.py` |

**文档状态**：§5.12 中记录了 `Weave` API，但实际代码是 Template API。

---

### 1.4 Notification API（5个端点）
| 方法 | 路径 | 功能 | 位置 |
|------|------|------|------|
| GET | `/api/v1/notifications` | 通知列表 | `app/router/notification.py` |
| GET | `/api/v1/notifications/unread-count` | 未读计数 | `app/router/notification.py` |
| POST | `/api/v1/notifications/{notification_id}/read` | 标记已读 | `app/router/notification.py` |
| POST | `/api/v1/notifications/read-all` | 全部已读 | `app/router/notification.py` |
| DELETE | `/api/v1/notifications/{notification_id}` | 删除通知 | `app/router/notification.py` |

**文档状态**：文档中未记录。

---

### 1.5 Subscription API（2个端点）
| 方法 | 路径 | 功能 | 位置 |
|------|------|------|------|
| GET | `/api/v1/subscriptions/plans` | 计划列表 | `app/router/subscription.py` |
| POST | `/api/v1/subscriptions/create-checkout` | 创建结算 | `app/router/subscription.py` |

**文档状态**：文档中未记录。

---

### 1.6 Admin API（3个端点）
| 方法 | 路径 | 功能 | 位置 |
|------|------|------|------|
| GET | `/api/v1/admin/llm-config` | 获取 LLM 配置 | `app/router/admin.py` |
| POST | `/api/v1/admin/llm-config` | 保存 LLM 配置 | `app/router/admin.py` |
| POST | `/api/v1/admin/llm-config/test` | 测试 LLM 连接 | `app/router/admin.py` |

**文档状态**：文档中未记录（但我们刚刚已经补充了 §8.1.1 和 §8.X）。

---

### 1.7 Card API 补充（2个端点）
文档中 Card API 只有 3 个端点，实际代码有 5 个：

| 方法 | 路径 | 功能 | 位置 |
|------|------|------|------|
| POST | `/api/v1/projects/:pid/cards/{card_id}/retire` | 卡牌退役 | `app/router/card.py` |
| GET | `/api/v1/projects/:pid/cards/history` | 抽卡历史 | `app/router/card.py` |

**文档状态**：§5.6 中未记录这两个端点。

---

### 1.8 Settings API 补充（2个端点）
文档中 Settings API 有 4 个端点，实际代码有 6 个：

| 方法 | 路径 | 功能 | 位置 |
|------|------|------|------|
| GET | `/api/v1/settings/profile` | 获取用户资料 | `app/router/setting.py` |
| PUT | `/api/v1/settings/profile` | 更新用户资料 | `app/router/setting.py` |

**文档状态**：§5.7 中未记录这两个端点。

---

## 二、文档记录了但未实现的 API

### 2.1 Import API（5个端点）
文档 §5.8 和 §5.12 中记录了 Import API：
- `POST /api/v1/projects/:pid/import`
- `GET /api/v1/projects/:pid/import/:jobid`
- `POST /api/v1/projects/:pid/import/:jobid/phase1`
- `POST /api/v1/projects/:pid/import/:jobid/phase2`
- `POST /api/v1/projects/:pid/import/:jobid/confirm`

**实际代码状态**：`app/router/` 中没有 `import.py`，但 `app/ingest/` 目录中有导入引擎实现。可能 API 还未实现，或者使用了不同的路径。

**建议**：检查 `app/ingest/` 中是否有 API 路由，如果没有，则标记为"待实现"。

---

### 2.2 Weave API（1个端点）
文档 §5.12 中记录了 Weave API：
- `GET /api/v1/weave-patterns`

**实际代码状态**：`app/router/weave.py` 中有 3 个端点：
- `GET /api/v1/weave/suggestions/{project_id}`
- `POST /api/v1/weave/apply`
- `GET /api/v1/weave/analyze/{project_id}`

**建议**：文档中记录的路径不正确，应该更新为实际的 API 路径。

---

### 2.3 Auth API 补充（1个端点）
文档 §5.12 中 Auth API 有 5 个端点（包括 `PUT /api/v1/auth/me`），但实际代码只有 4 个端点（没有 `PUT /me`）。

**建议**：从文档中删除 `PUT /api/v1/auth/me`，或者如果实际代码中 `GET /api/v1/auth/me` 也支持 PUT 方法，则补充到文档中。

---

## 三、需要执行的校正操作

### 3.1 补充缺失的 API 文档

1. **补充 Generation API**（§5.8 或新节）
2. **补充 Phase 4 API**（§5.9 或新节）
3. **补充 Template API**（替换 §5.9 Weave API）
4. **补充 Notification API**（新节）
5. **补充 Subscription API**（新节）
6. **补充 Admin API**（§8.1.1 已补充，但需要在 §5.12 中添加）
7. **补充 Card API 缺失的 2 个端点**（§5.6）
8. **补充 Settings API 缺失的 2 个端点**（§5.7）

### 3.2 修正错误的 API 文档

1. **修正 Weave API 路径**（§5.9 和 §5.12）
2. **删除 Auth API 中未实现的 `PUT /me`**（§5.2 和 §5.12）
3. **检查 Import API 是否已实现**（§5.8）

### 3.3 更新 API 端点总览（§5.12）

将实际实现的 71 个端点全部记录到 §5.12 中。

---

## 四、执行计划

1. **阶段 1**：补充缺失的 API 文档（Generation、Phase4、Template、Notification、Subscription、Admin）
2. **阶段 2**：修正错误的 API 文档（Weave、Auth、Import）
3. **阶段 3**：更新 API 端点总览（§5.12）
4. **阶段 4**：检查其他三个文档（前端设计文档、算法文档、后管规范）

---

**请确认**：
1. 这个对比报告是否准确？
2. 你希望我立即开始校正，还是先让你审查？
3. 对于"文档记录了但未实现的 API"，你希望标记为"待实现"还是直接删除？

告诉我你的选择，我会相应调整操作。
