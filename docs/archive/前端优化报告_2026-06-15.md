# 墨灵项目前端性能优化报告（归档）

生成时间：2026-06-15
状态：✅ 已完成，12个优化任务全部完成

## 执行概要

| 项目 | 数量 |
|:-----|-----:|
| 优化任务 | 12 |
| 覆盖组件 | 43个文件 |
| 优化模式 | 6种 |

## 优化模式摘要

| 模式 | 说明 | 应用文件 |
|:-----|:-----|:-----|
| React.memo | 浅比较 props，避免不必要的重渲染 | 12个UI/业务组件 |
| useMemo 稳定 Context value | Context value 每次渲染都是新对象，必须用 useMemo 稳定 | 4个Context |
| useRef 保存可变值 | 避免闭包过期和依赖重建 | WorkspaceContext |
| useEffect 依赖用原始值 | 禁止将对象/数组引用放入依赖数组 | RightPanel, LandingPage |
| 列表 key 用唯一标识 | 禁止用索引作为 key | WorldList 等 |
| API 请求去重 + 缓存 | 并发重复请求合并、GET 响应缓存 5s | apiClient.ts |

## 可复用模板

| 模板文件 | 可复用内容 |
|:-----|:-----|
| `src/lib/apiClient.ts` | 请求去重、响应缓存、Token 刷新去重 |
| `next.config.ts` | SWC 压缩、gzip、console 移除 |
| `src/app/globals.css` | content-visibility、prefers-reduced-motion |
| `src/app/globals.css`（设计令牌） | 完整的 CSS 变量系统 |

---
*归档时间：2026-06-17*
