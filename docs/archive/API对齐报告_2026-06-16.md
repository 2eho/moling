# 墨灵前后端 API 对齐报告（归档）

生成时间：2026-06-16
状态：✅ 已完成，10/10 不匹配项已修复

## 执行概要

| 项目 | 数量 |
|:-----|-----:|
| 发现的不匹配项 | 10 |
| 已修复项 | 10 |
| 剩余不匹配项 | 0 |

## 修复内容摘要

### 前端修改（api.ts）
- 删除 `weaveApi.getById`（后端无此端点）
- 重写 `importApi`（支持文本上传）
- 删除 `draftApi`（改用 `chapterApi.update`）
- 删除 `settingsApi` 项目级设置方法（后端无端点）
- 删除 `notificationsApi.deleteAllRead`（后端无端点）
- 删除 `subscriptionApi.cancel`（后端无端点）
- 删除 `adminApi` 中 5 个不匹配方法

### 后端修改
- `chapter.py` 添加 `redraw_chapter_cards()` 端点（占位符）
- `ingest/router.py` 支持 `UploadFile`

## 剩余工作

1. `redraw` 端点业务逻辑实现
2. 文件上传支持 Word/PDF 格式
3. 导入进度返回格式对齐

---
*归档时间：2026-06-17*
