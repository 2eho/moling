# ============================================================
# moling-server — 核心依赖
# ============================================================
#
# 基础依赖（标准库）: dataclasses, re, json, logging, time, random
# = 零额外依赖即可运行基础功能
#
# Web 爬取与正文提取（按需安装）:
#   pip install requests          — HTTP 采集
#   pip install beautifulsoup4    — HTML 解析（正文提取兜底）
#   pip install trafilatura       — 高质量正文提取

requests>=2.31.0
beautifulsoup4>=4.12.0
trafilatura>=2.0.0
