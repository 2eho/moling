# 墨灵项目 API 路由修复报告

> 报告人：backend-api-router-engineer
> 报告日期：2026-06-13
> 任务：修复 API 路由不匹配问题，并补充前端缺失的 API

---

## 执行概要

已完成模块 C（API 路由对齐）的全部 14 个问题的修复，以及模块 D（前端缺失 API）的部分修复工作。

---

## 一、模块 C：API 路由对齐（全部完成）

### ✅ C1 [HIGH] - 编辑类全部用 PUT 而非 PATCH
**修复内容**：
- 后端 `project.py`：`@router.patch` → `@router.put`
- 后端 `chapter.py`：`@router.patch` → `@router.put`
- 后端 `vault.py`：4 个 PATCH → PUT
- 前端 `api.ts`：所有 `apiClient.patch` → `apiClient.put`

### ✅ C2 [HIGH] - Chapter 前端路径补 project_id
**修复内容**：
- 前端 `api.ts`：`chapterApi.getById` 和 `update` 已包含 `projectId` 参数
- 路径格式：`/projects/${projectId}/chapters/${id}`

### ✅ C3 [HIGH] - 生成路径三方统一
**修复内容**：
- 后端 `generation.py`：路径从 `/trigger` 改为 `/projects/{project_id}/chapters/{chapter_id}/generate`
- 前端 `api.ts`：路径为 `/projects/${projectId}/chapters/${chapterId}/generate`

### ✅ C4 [HIGH] - 轮询生成状态路径统一
**修复内容**：
- 后端 `generation.py`：路径从 `/task/{task_id}` 改为 `/{task_id}/status`
- 前端 `api.ts`：路径从 `/generation/task/${taskId}` 改为 `/generation/${taskId}/status`

### ✅ C5 [HIGH] - 取消生成任务路径统一
**修复内容**：
- 后端 `generation.py`：路径从 `/task/{task_id}/cancel` 改为 `/{task_id}/cancel`
- 前端 `api.ts`：路径从 `/generation/task/${taskId}/cancel` 改为 `/generation/${taskId}/cancel`

### ✅ C6 [HIGH] - 重抽路径统一
**修复内容**：
- 前端 `api.ts`：`cardApi.redraw` 路径从 `/projects/${projectId}/cards/redraw` 改为 `/projects/${projectId}/chapters/${chapterId}/redraw`
- 添加了 `chapterId` 参数

### ✅ C7 [HIGH] - Import/Ingest 路径统一
**修复内容**：
- 前端 `api.ts`：`importApi` 路径从 `/projects/${projectId}/import/jobs` 改为 `/ingest/projects/${projectId}/jobs`
- 匹配后端 ing est router 的实际路径

### ✅ C8 [HIGH] - 卡牌池路径统一
**修复内容**：
- 后端 `router/__init__.py`：card router 挂载前缀从 `/api/v1/cards` 改为 `/api/v1/projects/{project_id}/cards`
- 后端 `card.py`：将 `project_id` 从 Query 参数改为 Path 参数

### ✅ C9 [MEDIUM] - 统计接口路径/语义对齐
**修复内容**：
- 后端 `project.py`：统计路径从 `/stats` 改为 `/{project_id}/stats`（项目级统计）
- 前端 `api.ts`：`projectApi.getStats` 改为接受 `projectId` 参数

### ✅ C10 [MEDIUM] - Vault 前端路径使用单数
**说明**：前端 `api.ts` 已使用单数路径（`/vault/timeline`, `/vault/world`），与后端一致。

### ✅ C11 [MEDIUM] - 注册接口字段名统一
**修复内容**：
- 后端 `schemas/auth.py`：`RegisterReq` 中的 `username` 字段改为 `nickname`
- 前端 `api.ts` 已使用 `nickname` 字段

### ✅ C12 [MEDIUM] - 密码重置路径名统一
**修复内容**：
- 后端 `auth.py`：路径从 `/reset-password` 改为 `/password-reset-request`
- 匹配 API 映射文档

### ✅ C13 [MEDIUM] - 抽卡路由架构统一
**修复内容**：
- 后端 `router/__init__.py`：card router 挂载到 `/api/v1/projects/{project_id}/cards` 前缀下
- 所有卡牌相关路径 now 包含 `project_id`

### ✅ C14 [MEDIUM] - 生成路由身份验证
**修复内容**：
- 后端 `generation.py`：为 `get_task_status` 和 `cancel_generation_task` 添加 `current_user` 依赖
- 确保只有认证用户才能轮询或取消任务

---

## 二、模块 D：前端缺失 API（部分完成）

### ✅ D1 [MEDIUM] - Settings 全部 5 个 API
**状态**：后端已实现，前端已修复
**修复内容**：
- 后端 `setting.py`：将 PATCH 改为 PUT（遵循 C1 统一）
- 前端 `api.ts`：已定义 `settingsApi` 对象，包含全部 6 个方法

### ✅ D2 [MEDIUM] - Notifications 全部 3 个 API
**状态**：后端已实现，前端已修复
**修复内容**：
- 前端 `api.ts`：将 `markAsRead` 和 `markAllAsRead` 从 PUT 改为 POST（匹配后端）
- 后端使用 POST 对于状态变更操作是合理的

### ✅ D3 [MEDIUM] - Subscription 全部 2 个 API
**状态**：后端已实现，前端已修复
**修复内容**：
- 前端 `api.ts`：修复路径从 `/subscription/` 改为 `/subscriptions/`（匹配后端路由挂载前缀）

### ⚠️ D6 [MEDIUM] - Vault Summary 后端补充（进行中）
**状态**：router 已添加，service 层待实现
**修复内容**：
- 后端 `vault.py`：添加 `GET /summary` 端点
- 待实现：`vault_service.get_vault_summary()` 函数

---

## 三、文件修改清单

### 后端文件（9 个）
1. `moling-server/app/router/__init__.py` - 修改路由挂载前缀
2. `moling-server/app/router/project.py` - C1, C9
3. `moling-server/app/router/chapter.py` - C1
4. `moling-server/app/router/vault.py` - C1, D6
5. `moling-server/app/router/generation.py` - C3, C4, C5, C14
6. `moling-server/app/router/card.py` - C8, C13
7. `moling-server/app/router/auth.py` - C11, C12
8. `moling-server/app/router/setting.py` - D1
9. `moling-server/app/schemas/auth.py` - C11

### 前端文件（1 个）
1. `moling-web/src/lib/api.ts` - C1, C2, C4, C5, C6, D2, D3

---

## 四、待完成任务

### 高优先级
1. **D6 补充** - 实现 `vault_service.get_vault_summary()` 函数
2. **后端 API 验证** - 启动后端服务，验证所有修改后的路由是否正确注册
3. **前端集成测试** - 启动前端服务，测试 API 调用是否成功

### 中优先级
4. **D4/D5/D7-D14 验证** - 验证前端 API 调用是否匹配后端路径
5. **审计报告更新** - 更新 `统一审计报告_墨灵全项目.md`，标记已修复的条目

---

## 五、测试建议

### 1. 后端路由验证
```bash
# 启动后端服务
cd moling-server
uvicorn app.main:app --reload

# 访问 Swagger 文档
open http://localhost:8000/docs

# 验证以下路径是否正确注册：
# - PUT /api/v1/projects/{id}
# - PUT /api/v1/projects/{project_id}/chapters/{id}
# - POST /api/v1/projects/{project_id}/chapters/{chapter_id}/generate
# - GET /api/v1/generation/{task_id}/status
# - POST /api/v1/generation/{task_id}/cancel
# - POST /api/v1/projects/{project_id}/cards/draw
# - GET /api/v1/projects/{project_id}/cards/pool
```

### 2. 前端 API 调用测试
```bash
# 启动前端服务
cd moling-web
npm run dev

# 测试关键流程：
# 1. 用户注册/登录
# 2. 创建项目
# 3. 创建章节
# 4. 抽卡 → 生成 → 轮询 → 确认
# 5. Vault CRUD
# 6. 设置更新
```

---

## 六、已知问题

1. **generation router 路径问题** - 触发路径定义在 generation router 中，但语义上应该属于 chapter router。建议后续重构。
2. **Vault Summary service 层未实现** - 添加了 router 端点，但 service 层和 DAO 层还需要实现。
3. **部分 API 仍是 stub** - 如 `subscription.create_checkout` 返回 "Coming soon"。

---

## 七、附录：修改统计

- **总修改文件数**：10 个
- **后端文件**：9 个
- **前端文件**：1 个
- **修复问题数**：14 个（模块 C 全部）
- **部分修复**：3 个（模块 D 的 D1, D2, D3）
- **进行中**：1 个（D6）

---

**报告结束**

请 team-lead 审查本次修复工作，并安排后续测试和优化工作。
