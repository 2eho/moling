> ⚠️ 注意：截至 2026-06-14，此文档所述功能尚未在代码中完全实现。
> algorithm_service.py / prompt_service.py / validation_service.py 均为空壳 stub。
> 请参阅 `moling-server/app/service/` 下对应文件确认最新状态。

# 墨灵项目 · 开发文档变更日志

> **创建日期**：2026-06-14  
> **覆盖文档**：  
> - `012_a7c27b64_墨灵后端设计文档.md`  
> - `009_2b7b5b03_moling-card-combination-algorithm.md`  
> - `001_b8542cf6_后台管理-设计系统规范.md`  
> - `004_79b91a8b_前端设计系统-主文档.md`  
> **目的**：记录对开发文档的所有更新，确保文档与代码实现保持同步。

---

## 一、后端实现更新（2026-06-13）

### 1.1 LLM 配置管理（后端设计文档 §8）

**实现文件**：
- `moling-server/app/router/admin.py` - LLM 配置 API 端点
- `moling-server/app/config.py` - 数据库覆盖机制

**新增 API 端点**：
- `GET /api/v1/admin/llm-config` - 获取当前 LLM 配置（API Key 脱敏显示）
- `POST /api/v1/admin/llm-config` - 保存 LLM 配置到数据库并更新运行时覆盖
- `POST /api/v1/admin/llm-config/test` - 测试 LLM API 连接

**核心机制**：
- **数据库覆盖 > 环境变量**：`config.py` 中的 `_OVERRIDES` 字典允许运行时动态覆盖配置
- **脱敏显示**：`_mask_key()` 函数只显示 API Key 的前 4 和后 4 个字符
- **立即生效**：`set_override()` 更新内存中的配置，下次 LLM 调用时生效

**需要更新到后端设计文档的内容**：
- 在 §8.1 添加关于 LLM 配置管理的内容
- 在 §8.11 确认 API Key Pool 策略是否与实现一致

---

### 1.2 LLM 客户端（后端设计文档 §8）

**实现文件**：
- `moling-server/app/llm/client.py` - 统一 LLM 客户端

**核心功能**：
- **统一客户端**：`LLMClient` 类支持 OpenAI 兼容的 API 格式
- **流式和非流式支持**：`chat()` 和 `chat_stream()` 方法
- **自动重试**：使用 `tenacity` 库实现指数退避重试（最多 3 次）
- **运行时配置读取**：每次调用时从 `get_effective_llm_config()` 读取配置（支持数据库覆盖）
- **Token 计数**：`count_tokens()` 方法提供近似 Token 计数（4 字符 ≈ 1 Token）

**需要更新到后端设计文档的内容**：
- 在 §8.9 添加关于 `LLMClient` 的实现细节
- 确认 §8.3 Fallback 链配置是否与 `LLMClient` 的重试逻辑一致

---

### 1.3 Phase 4 调度器（后端设计文档 §7）

**实现文件**：
- `moling-server/app/service/phase4_service.py` - Phase 4 服务实现
- `moling-server/app/worker/phase4_task.py` - Phase 4 Celery 任务（待确认）

**核心功能**（根据 CHANGELOG-2026-06-13.md）：
- **Phase 4 状态机**：`Phase4Scheduler` 实现 IDLE → RUNNING → DONE/FAILED 状态转换
- **22 步全流程管线**：`algorithm_service.py` 中的 `run_full_pipeline()` 真实实现所有步骤
- **四库自动收纳**：Step 14-22 真实实现（LLM 提取变更、更新四库、提取秘密、充实卡牌池、健康检查、归档日志）

**需要更新到后端设计文档的内容**：
- 在 §7.3 更新 Phase 4 状态机的实现细节
- 在 §7.4 更新 Phase 4 完整执行流程
- 确认 §7.5 写入锁机制是否与实现一致

---

### 1.4 卡牌生命周期和抽卡状态机（算法文档 §4）

**实现文件**：
- `moling-server/app/service/card_service.py` - 卡牌服务实现

**核心功能**（根据 CHANGELOG-2026-06-13.md）：
- **卡牌生命周期**：`check_freshness()`、`check_retirement()`、`check_elimination()` 真实实现
- **抽卡状态机**：`DrawStateMachine.transition()` 实现 IDLE → DRAWING → DRAWN → REDRAWING → COMPLETED 状态转换
- **加权抽卡算法**：`get_weighted_cards()` 实现 rarity_weight × freshness × pity 加权
- **重抽机制**：`redraw_cards()` 检查重抽上限、排除当前卡片、重新抽卡

**需要更新到算法文档的内容**：
- 在 §4.0 更新卡牌池生命周期状态图（确认是否与实现一致）
- 在 §4.1 更新抽卡完整状态机（确认是否与实现一致）

---

### 1.5 子情节健康监控（算法文档 §5.3）

**实现文件**：
- `moling-server/app/service/health_service.py` - 健康服务实现

**核心功能**（根据 CHANGELOG-2026-06-13.md）：
- **R1 规则**：8 章未推进告警（🟡 黄色）
- **R2 规则**：4+ 同类型重复推进行为（🟠 橙色）
- **R3 规则**：10 章静默承诺告警（🔴 红色）
- **辅助方法**：`_get_all_promises()`、`_calculate_promise_age()`、`_is_critical_promise()`、`_calculate_health_score()`、`_save_alert()` 真实实现

**需要更新到算法文档的内容**：
- 在 §5.3.2 更新三条健康规则的 SQL 实现（确认是否与 `health_service.py` 一致）

---

## 二、算法实现更新（2026-06-13）

### 2.1 风格指纹系统（算法文档 §9.5）

**实现文件**：
- `novel_dissector/core/style_analyzer.py` - 文风指纹分析器

**核心功能**：
- **7 维文风指纹**：`StyleFingerprint` 数据类包含句式复杂度、对话占比、段落节奏、时态/视角、标点特征等维度
- **Step 1 维度（零依赖）**：纯正则 + 标准库统计，包括平均句长、句长标准差、对话段落比例、段落长度分布、人称代词比例、标点密度
- **Step 2 维度（需 jieba）**：词汇偏好（TF-IDF Top 高频实词）、内容分类比（描写/叙事/心理/对话的比例）
- **分析入口**：`analyze_style()`（从文本分析）、`analyze_style_from_chapters()`（从章节列表分析）、`analyze_style_from_result()`（从 DissectResult 分析）

**需要更新到算法文档的内容**：
- 在 §9.5 添加关于 `StyleFingerprint` 数据结构的详细说明
- 在 §3.5 Layer 4 确认风格约束注入是否与实现一致

---

### 2.2 Genre Profile A5（算法文档 §9.5）

**实现文件**：
- `moling-server/app/genre/a5_profile_output.py` - A5 套路归纳 + 去版权化 + Genre Profile 全量输出

**核心功能**：
- **A5 套路归纳**：`A5_summarize_patterns()` 函数汇总 A1-A4 分析结果，生成完整的 GenreProfile
- **去版权化过滤**：`_filter_copyrighted()` 函数移除受版权保护的角色名/设定名
- **GenreProfile 生成**：包括黄金三章结构、角色原型、节奏曲线、世界观模板、钩子密度、卡牌池预填等字段
- **辅助函数**：`_extract_world_templates()`（提取世界观模板）、`_pattern_label()`（模式标签）、`_generate_card_seeds()`（生成初始卡牌方向）

**需要更新到算法文档的内容**：
- 在 §9.5 更新 Genre Profile 数据结构的字段说明（确认是否包含 A5 新增字段）
- 在 §9.4 更新拆书引擎的 A1-A5 流程说明

---

### 2.3 API Key Pool（后端设计文档 §8.11）

**文档位置**：
- `012_a7c27b64_墨灵后端设计文档.md` §8.11 - API Key Pool：多 Key 轮询与限速管理

**需要确认的内容**：
- §8.11 是否已经记录了 API Key Pool 的实现策略？
- 如果没有，需要添加关于 Pro Pool（9 Keys）和 Flash Pool（6 Keys）的轮询策略、限速管理、成本优化等内容。

---

## 三、前端实现更新（待确认）

### 3.1 后台管理页面（设计系统规范 001）

**需要确认的内容**：
- 是否已经实现了 LLM 配置管理页面（对应后端的 `/admin/llm-config` 端点）？
- 如果已经实现，需要更新 `001_b8542cf6_后台管理-设计系统规范.md`，添加关于 LLM 配置页面的组件规格。

---

### 3.2 前端 API 调用（前端设计系统 004）

**需要确认的内容**：
- `moling-web/src/lib/api.ts` 是否已经更新，以支持新的后端 API（如 LLM 配置、Settings、Notifications 等）？
- 如果已经更新，需要更新 `004_79b91a8b_前端设计系统-主文档.md`，添加关于这些 API 的前端调用逻辑。

---

## 四、待完成任务

1. **确认 algorithm_service.py 的实际实现**：当前读取的文件是最小化占位符，需要找到完整的实现文件（可能在其他位置，或者需要重新生成）。
2. **确认 API Key Pool 的实际实现**：在代码中未找到明确的 Key Pool 实现，需要确认是否只在文档中设计了策略，还是已经有代码实现。
3. **更新后端设计文档 §8**：添加关于 LLM 配置管理、LLM 客户端、API Key Pool 的实现细节。
4. **更新算法文档 §4 和 §5**：确认卡牌生命周期、抽卡状态机、健康监控的实现是否与文档一致。
5. **更新算法文档 §9**：添加关于风格指纹系统、Genre Profile A5 的实现细节。
6. **更新前端设计文档**：根据前端实际实现，更新相应的组件规格和 API 调用逻辑。

---

**日志创建人**：郝交付（Delivery Director）  
**下次更新**：确认上述"待完成任务"的进展后更新
