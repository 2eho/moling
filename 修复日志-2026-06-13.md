# 墨灵项目修复日志 - 2026-06-13

> 本文档记录所有修复内容，方便下次审查。

---

## 第一轮修复（2026-06-13 20:00 - 21:30）

### 关键问题修复

| # | 问题 | 文件 | 修复内容 | 状态 |
|---|------|------|----------|------|
| 1 | User 模型缺少 Text 导入 | `app/models/user.py` | 在 import 语句添加 `Text` | ✅ |
| 2 | Phase 4 导入错误 | `app/service/chapter_service.py` | 改为导入 `phase4_service_fixed` | ✅ |
| 3 | Chapter.phase4_status 默认值错误 | `app/models/chapter.py` | `"none"` → `"pending"` | ✅ |
| 4 | CardPool 缺少关键字段 | `app/models/card_pool.py` | 添加 `source_label`, `pick_count`, `last_drawn_chapter` | ✅ |

### 算法服务修复（11 个步骤）

| 步骤 | 方法名 | 修复内容 | 状态 |
|------|--------|----------|------|
| 3 | `step3_conflict_detection` | 添加状态机冲突检测、置信度评分、LLM fallback | ✅ |
| 4 | `step4_direction_conflict_score` | 改进实体提取、添加置信度评分 | ✅ |
| 5 | `step5_weaving_match` | 添加 LLM fallback、全选三卡逻辑 | ✅ |
| 10 | `step10_consistency_check` | 添加承诺推进检查、世界观规则检查、秘密一致性检查 | ✅ |
| 11 | `step11_dynamic_layer_update` | 使用 LLM 提取锚点、更新连贯性基线、更新最近变更 | ✅ |
| 14 | `step14_vault_change_extraction` | 添加详细 JSON Schema、SourceText Grounding、实体名规范化 | ✅ |
| 15 | `step15_character_update` | 添加 action 类型、模糊匹配、用户审查队列 | ✅ |
| 16 | `step16_timeline_update` | 添加 action 类型、矛盾检查、事件关系链 | ✅ |
| 17 | `step17_plot_promise_update` | 添加 action 类型、event_type 自动标注 | ✅ |
| 19 | `step19_secret_extraction` | 实现秘密生命周期、债务模型 | ✅ |
| 20 | `step20_card_pool_enrichment` | 实现稀有度分配规则、质量门控 | ✅ |

### API 修复

| # | 问题 | 文件 | 修复内容 | 状态 |
|---|------|------|----------|------|
| 5 | redraw 端点逻辑不完整 | `app/router/chapter.py` | 实现实际重抽逻辑（调用 CardService） | ✅ |
| 6 | health refresh 响应格式不符 | `app/router/project.py` | 修改响应格式为 `{alerts, checked_at}` | ✅ |
| 7 | secrets 端点参数类型不匹配 | `app/router/secret.py` | 添加 GET `/character/{character_name}` 端点 | ✅ |
| 8 | 缺失编辑秘密端点 | `app/router/secret.py` | 添加 PATCH `/{secret_id}` 端点 | ✅ |
| 9 | 缺失全量重新分析端点 | `app/router/vault.py` | 添加 POST `/full-analyze` 端点（桩实现） | ⚠️ |
| 10 | HTTP 方法不一致 | `app/router/setting.py` | PUT → PATCH | ✅ |
| 11 | 导入 API 路径不一致 | `app/router/__init__.py` | 待修复 | 🔄 |

---

## 第二轮审计（2026-06-13 21:30 - 22:00）

**发现的新问题**:
- reaudit-algorithm: 4 个语法错误（字典缺少逗号、正则错误、函数调用缺少逗号、硬编码路径）
- reaudit-api: 3 个问题（settings 主端点仍为 PUT、health refresh 缺少导入、full-analyze 为桩实现）
- reaudit-models: ✅ 所有模型修复完整

### 紧急修复（第二轮发现问题）

| # | 问题 | 文件 | 修复内容 | 状态 |
|---|------|------|----------|------|
| 12 | algorithm_service.py 语法错误 | `app/service/algorithm_service.py` | 修复字典逗号、正则、函数调用逗号 | ⚠️ 代理报告语法错误不存在 |
| 13 | 硬编码路径 | `app/service/algorithm_service.py` | 使用配置路径 `ARCHIVE_DIR` | ✅ |
| 14 | settings 主端点 HTTP 方法 | `app/router/setting.py` | `PUT` → `PATCH` | ✅ |
| 15 | health refresh 缺少导入 | `app/router/project.py` | 添加 `from datetime import datetime, timezone` | ✅ |
| 16 | full-analyze 返回消息 | `app/router/vault.py` | 改进返回消息 | ✅ |

---

## 第三轮审计（进行中）

**目标**: 验证第二轮修复是否完整，发现剩余问题。

**审计范围**:
- [ ] 验证 algorithm_service.py 语法是否正确（尝试启动服务器）
- [ ] 验证 API 修复是否完整
- [ ] 验证所有修复是否引入新问题

---

## 待办事项

- [ ] 完成第三轮审计
- [ ] 修复第三轮审计发现的问题
- [ ] 循环审计直到零问题
- [ ] 更新 CHANGELOG.md

---

## 审计循环记录

| 轮次 | 日期 | 发现问题时 | 修复问题时 | 剩余问题时 |
|-------|------|------------|------------|------------|
| 1 | 2026-06-13 20:00 | ~70+ | 11 | ~7 |
| 2 | 2026-06-13 21:30 | 7 | 5 | ? |
| 3 | 进行中 | ? | ? | ? |

---

_本文档会自动更新，每次修复后添加新的记录。_
