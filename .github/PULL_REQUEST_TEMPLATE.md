## 概述

<!-- 简要描述这次改动的目的，为什么需要这个改变 -->

## 改动类型

<!-- 勾选对应的类型 -->

- [ ] ✨ 功能新增 (`feat`)
- [ ] 🐛 Bug 修复 (`fix`)
- [ ] 🔧 重构 (`refactor`)
- [ ] 📝 文档 (`docs`)
- [ ] ✅ 测试 (`test`)
- [ ] 🎨 样式 (`style`)
- [ ] ⚙️ 杂务/CI (`chore`)
- [ ] ⚡ 性能优化 (`perf`)

## 具体改动

<!--
列出关键改动点，方便 reviewer 聚焦审查
示例：
- 新增 `src/lib/cardWeight.ts` 中的权重计算函数
- 修改 `CardDrawPanel.tsx` 的抽卡逻辑
- 移除不再需要的 `old-function.ts`
-->

## 测试验证

- [ ] 本地构建通过（`npm run build` / `pip install -e .`）
- [ ] 后端测试通过（`python -m pytest`）
- [ ] 前端编译通过（`npm run build`）
- [ ] Lint 检查通过
- [ ] 手动测试完成

## 截图（UI 改动时必填）

<!-- 插入前后对比截图，方便 reviewer 直观理解改动效果 -->

| 改动前 | 改动后 |
|--------|--------|
|        |        |

## 相关 Issue

<!-- Closes #123 -->
